import 'package:flutter/widgets.dart';

/// Displays success message when receiving referral reward.
class ReferralRewardsHandler extends StatelessWidget {
  final Widget child;
  const ReferralRewardsHandler({super.key, required this.child});

  @override
  Widget build(BuildContext context) => child;
}
